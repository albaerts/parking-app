// Obsolete legacy component retained only as placeholder to prevent build errors if imported elsewhere.
// The email verification flow now uses backend redirect to /email-verified and the EmailVerified component in App.js.
// Safe to delete after confirming no imports reference it.
export default function VerifyEmail() { return null; }
