# Production Environment Variables Sample
# Copy to .env.prod and adjust values. Do NOT commit real secrets.

# --- Core JWT/Auth ---
JWT_SECRET=replace_with_long_random_string
JWT_ALGO=HS256
JWT_EXP_MINUTES=60

# --- Database ---
# For Postgres (recommended in production):
# Format: postgresql+psycopg2://USER:PASSWORD@HOST:5432/DBNAME
DATABASE_URL=postgresql+psycopg2://parking:parking_pass@postgres:5432/parkingdb
# If you insist on SQLite (single instance only):
# DATABASE_URL=sqlite:////app/backend/parking.db

# --- Email / Microsoft Graph (optional, for verification emails) ---
AZURE_CLIENT_ID=
AZURE_CLIENT_SECRET=
AZURE_TENANT_ID=
MAIL_FROM=no-reply@parking.gashis.ch
# Set to 'true' only in staging if you want accounts auto-verified when email send fails
AUTO_VERIFY_ON_EMAIL_FAILURE=false

# --- Rate Limits / Performance (future hooks) ---
# MAX_AUTOCOMPLETE_PER_MINUTE=30

# --- Feature Flags ---
# ENABLE_BUSINESS_SEARCH=false

# --- Logging ---
# LOG_LEVEL=info

# --- Postgres container defaults (match docker-compose.prod.yml if overriding) ---
POSTGRES_USER=parking
POSTGRES_PASSWORD=parking_pass
POSTGRES_DB=parkingdb

# --- Runtime Domain (optional; can be used by frontend or CORS tightening) ---
PRIMARY_DOMAIN=parking.gashis.ch
API_DOMAIN=parking.gashis.ch

# Note: For GitHub Actions remote deploy, secrets are provided differently (repository secrets).
