JWT_SECRET=change_me_to_a_long_random_secret
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB=parking
# Set to 1 to force in-memory mode (no DB). Use 0 for real Mongo.
FORCE_MEMORY_MODE=1
# Optional secret for memory command queue endpoint
SIMPLE_COMMAND_SECRET=
