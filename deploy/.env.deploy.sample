# Beispiel-Variablen für ./deploy/manual_deploy.sh
# Kopiere zu deploy/.env.deploy und passe an

# Zielserver
SSH_HOST=1.2.3.4
SSH_USER=deploy
SSH_PORT=22
REMOTE_PATH=/var/www/parkingsrv

# Container-Registry
REGISTRY=ghcr.io
REPO_OWNER=albaerts
# Optional: Personal Access Token, falls Images privat sind
# GHCR_TOKEN=

# Public URL für Sanity-Check (optional)
PUBLIC_URL=https://parking.gashis.ch/

# Datenbank (Produktion: Postgres verwenden)
# Wird als Default im docker-compose.prod.yml gesetzt, kann hier überschrieben werden.
# DATABASE_URL=postgresql+psycopg2://parking:parking_pass@postgres:5432/parkingdb

# Optional: Postgres-Variablen, falls von den Defaults abweichend
# POSTGRES_USER=parking
# POSTGRES_PASSWORD=parking_pass
# POSTGRES_DB=parkingdb

# Optional: falls Repo noch nicht auf dem Server liegt
# GIT_CLONE_URL=git@github.com:albaerts/parking-app.git

# Optional: lokales Env-File, das auf den Server hochgeladen wird
LOCAL_ENV_FILE=.env.prod
REMOTE_ENV_FILE=.env.prod
